print("GITHUBRAW")
print("GITHUBRAW")
print("GITHUBRAW")
print("GITHUBRAW")
print("GITHUBRAW")
print("GITHUBRAW")
print("GITHUBRAW")
print("GITHUBRAW")

IOUsetting = {
	"ripotQWEj",
	"Noob"
}

for i, v in ipairs(IOUsetting) do
	print(i, v)
end



if table.find(IOUsetting, player.Name) then
	Cheak.Visible = false
	search.Visible = true
	st1.Visible = true
	st2.Visible = true
	st3.Visible = true
	ScriptFrame.Visible = true
	scrlingframe2.Size = UDim2.new(0, 150, 1, 40)
	SecurytiFrame.Visible = false
	DiscordFrame.Visible = false
else
	TextSecurity.Text = "whitelist dont found you! go to discord!"
	TextSecurity.Size = UDim2.new(0.9,0,0.4,0)
	local Link = Instance.new("TextButton")
	Link.Size = UDim2.new(0.8, 0, 0.12, 0)
	Link.Position = UDim2.new(0.5, -100, 0.5, -25)
	Link.Text = "Copy Link"
	Link.BackgroundColor3 = Color3.new(0.3, 0.3, 0.3)
	Link.Parent = SecurytiFrame
	Link.BorderColor3 = Color3.new(0, 0, 0)
	Link.BorderSizePixel = 1
	Link.BorderColor3 = black
	Link.TextScaled = true
	Link.TextStrokeTransparency = 1
	Link.TextColor3 = white

	local linklable = Instance.new("TextLabel")
	linklable.Size = UDim2.new(0.8, 0, 0.12, 0)
	linklable.Position = UDim2.new(0.5, -100, 0.5, -25)
	linklable.Text = "https://discord.gg/Z9R2h8Uw"
	linklable.BackgroundTransparency = 1
	linklable.Parent = SecurytiFrame
	linklable.BorderColor3 = black
	linklable.TextScaled = true
	linklable.TextColor3 = white
	linklable.TextStrokeTransparency = 0

	scrlingframe2.Size = UDim2.new(0, 150, 1, 40)
	SecurytiFrame.Size = UDim2.new(1, -150, 1, 40)
	DiscordFrame.Size = UDim2.new(1, -150, 1, 40)
	ScriptFrame:Destroy()

	Link.Activated:Connect(function()
		if setclipboard then
			Link.Text = "LINK COPY!"
			setclipboard("https://discord.gg/Z9R2h8Uw")
			wait(0.5)
			Link.Text = "Copy Link"
		else
			Link.Text = "error, use other executor"
			wait(2)
			Link.Text = "Copy Link"
		end
	end)

	st1:Destroy()
	ScriptFrame:Destroy()

	return
end
